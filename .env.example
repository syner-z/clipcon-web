# 선택 항목: 기본은 같은 오리진의 /api (vite dev 서버가 /api를 백엔드로 프록시한다).
# 프런트와 백엔드를 다른 오리진으로 띄울 때만 절대 경로로 지정한다.
# VITE_API_BASE=http://localhost:8000/api

# 선택 항목: 비워 두면 Google Analytics를 아예 내려받지 않는다(개발 기본값).
# 운영 배포에서만 GA4 측정 ID를 채운다.
# VITE_GA_ID=G-XXXXXXXXXX
